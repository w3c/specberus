exports.name = 'CRYD';
exports.config = {
    status: 'CRYD',
    longStatus: 'Candidate Registry Draft',
    styleSheet: 'CRYD',
    track: 'RY',
};

// const base = require('../base');

const { rules } = require('./CRY');

exports.rules = rules;
