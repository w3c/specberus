exports.name = 'CRYD';
exports.config = {
    status: 'CRYD',
    longStatus: 'Candidate Registry Draft',
    styleSheet: 'CRYD',
    track: 'Registry',
};

const { rules } = require('./CRY');

exports.rules = rules;
