exports.name = 'CRY';
exports.config = {
    status: 'CRY',
    longStatus: 'Candidate Registry',
    styleSheet: 'CRY',
    track: 'RY',
};

// const base = require('../base');

const { rules } = require('./RY');

exports.rules = rules;
